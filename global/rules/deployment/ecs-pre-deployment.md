# ECS Deployment - Pre-Deployment Verification

**MANDATORY checks before pushing:**

```bash
npm test        # All tests must pass
npm run lint    # No errors (warnings OK)
npm run format  # Code formatted
npm run build   # Build must succeed
```
